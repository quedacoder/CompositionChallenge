package com.quedacoder;

public class WestWall extends Wall {
    public WestWall(Dimensions dimensions, boolean isWindowPresent, boolean isPlugPresent, String color) {
        super(dimensions, isWindowPresent, isPlugPresent, color);
    }
}
